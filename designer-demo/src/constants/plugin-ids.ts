/**
 * Custom left-panel plugin IDs used by designer-demo / lowcode-kit.
 * Official META_APP.* entries for these plugins are disabled in registry.ts.
 */

/** Replaces official META_APP.Page (engine.plugins.pagecontroller) */
export const SCRIPT_PLUGIN_ID = 'engine.plugins.customScript';
