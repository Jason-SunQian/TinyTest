---
description: 设计器改造不得修改 packages，packages 仅作参考
globs: designer-demo/**/*
alwaysApply: false
---

# 设计器与 packages 的边界约定

## 核心原则

**设计器（designer-demo）的改造一律不得修改 `packages` 目录下的任何文件。**

## 原因

- **依赖来源**：设计器实际依赖的是通过 npm 安装的 `@opentiny/tiny-engine-*` 等包，而非本仓库的 `packages` 源码。
- **packages 定位**：`packages` 是 TinyEngine 官方源码的本地副本，**仅用于参考和阅读**，不参与设计器的构建与运行。
- **维护成本**：修改 packages 会导致与 npm 包版本不一致，升级依赖时改动会丢失，且无法随设计器独立迭代。

## 正确做法

1. **插件迁移**：从 `packages/plugins/*` 复制到 `designer-demo/src/plugins/*`，在 `registry.ts` 中注册自定义入口，禁用官方插件。
2. **功能扩展**：在 `designer-demo/src/` 内实现覆盖或包装逻辑，通过 Registry 覆盖官方入口。
3. **参考实现**：需要了解行为时，阅读 `packages` 源码，但**不直接修改**。

## 参考文档

- `designer-demo/docs/PLUGIN_MIGRATION.md` - 插件迁移（复制到 designer-demo，不修改 packages）
- `designer-demo/docs/CODE_COMPLETION_GUIDE.md` - 代码提示扩展（不修改 packages）
- `designer-demo/docs/styles-plugin-migration.md` - 样式插件迁移（避免修改 packages）
- `designer-demo/docs/I18N_EXPERIENCE_SUMMARY.md` - 国际化（packages 仅作参考）
